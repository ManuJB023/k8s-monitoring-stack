apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
name: monitoring
nodes:
  - role: control-plane
    image: kindest/node:v1.30.0
    extraPortMappings:
      - containerPort: 30000
        hostPort: 30000
        protocol: TCP
